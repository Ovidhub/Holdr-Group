/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor

# Environment & secrets — never commit
.env
.env.backup
.env.*.bak
*.bak

# Database dumps / local databases (contain real user data & password hashes)
db.sql
*.sql
*.sqlite
# ...but keep the safe branding script (no user data)
!database/branding-mortil-holders.sql

.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode

# Local dev server router
serve-local.php

# Local screenshots / playwright artifacts
.playwright-mcp/
mortil-home.png
